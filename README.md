# string-palindrome-checker
A simple program to check whether a given string is a palindrome (reads the same forward and backward).
# 🔁 String Palindrome Checker

This is a simple C++ program to check whether a given string is a palindrome.

## 📌 What is a Palindrome?
A palindrome is a word, phrase, or sequence that reads the same backward as forward.

### Examples:
- madam ✅
- racecar ✅
- hello ❌

## 🚀 Features
- Checks if a string is palindrome
- Efficient two-pointer approach
- Beginner-friendly code

## 🛠️ Technologies Used
- C++

## ▶️ How to Run
1. Clone the repository
2. Compile the code:
   ```bash
   g++ palindrome.cpp -o palindrome
